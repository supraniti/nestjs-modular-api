/**
 * Placeholder E2E for MongoDB module.
 * This module is internal-only (no HTTP), so we keep the file skipped.
 * When a higher-level HTTP module is built on top, we’ll enable proper E2E here.
 */
describe.skip('MongoDB E2E (placeholder)', () => {
  it('noop', () => {
    expect(true).toBe(true);
  });
});
